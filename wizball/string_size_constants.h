#define NAME_SIZE				(64) // Used for short single words
#define MAX_LINE_SIZE			(512) // User for lines of text from files
#define TEXT_LINE_SIZE			(1024)
#define TEXTFILE_SUPER_SIZE		(65536) // Used for the descriptive text

#define OKAY					(1)
#define UNSET					(-1)
#define VERY_UNSET				(-2)
#define BROKEN_LINK				(-2)
#define VERY_BROKEN_LINK		(-3)
