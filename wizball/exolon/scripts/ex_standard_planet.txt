// This script is called by any entity like the rocket, bio-growth
// or whatever which does nothing apart from get blown up. Saves on
// duplication of code.

#define planet_size 		v0
#define planet_width 		v1
#define planet_size_small	0
#define planet_size_medium	1
#define planet_size_large	2

#define planet_base_x		v20
#define planet_base_y		v21

	let planet_width = planet_size * 8
	let planet_width = planet_width + 48

	let current_frame = base_frame
	let draw_mode = DRAW_MODE_SPRITE

	if parent != CONST_UNSET then
		CHANGE_SCRIPT ex_planet_orbitter
	end_if

	let planet_base_x = x
	let planet_base_y = y

start

end
