	let Bitshift = 4
	let x_vel = x_vel << bitshift
	let y_vel = y_vel << bitshift

	let v1 = rand -24 24
	let x_vel = x_vel + v1

	let v1 = rand -48 24
	let y_vel = y_vel + v1

	SET_PRIVATE_CO_ORDS
	let sprite = level_scenery_sprites[arb]
	let current_frame = base_frame
	let draw_mode = DRAW_MODE_SPRITE
	let opengl_booleans = OPENGL_BOOLEAN_BLEND_ADD
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_SCALE
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_ROTATE

	let v0 = 100
	let opengl_angle = rand 0 36000
	let v1 = rand -500 500

	let y_acc = 3
	
start
	let opengl_scale_x = opengl_scale_x % 9800
	let opengl_scale_y = opengl_scale_y % 9800

	let opengl_angle = opengl_angle + v1

	let v0 = v0 - 1

	if v0 = 0 then
		kill_entity own_id
	end_if
end