#define progress		v30

let Bitshift = 0
SET_PRIVATE_CO_ORDS
let sprite = nebula_background[arb]
let current_frame = 1
let interpolated_frame = 2
let draw_mode = DRAW_MODE_INTERP_SPRITE
let OPENGL_BOOLEANS = OPENGL_BOOLEANS + OPENGL_BOOLEAN_FILTERED
let OPENGL_BOOLEANS = OPENGL_BOOLEANS + OPENGL_BOOLEAN_SCALE

let OPENGL_SCALE_X = 50000
let OPENGL_SCALE_Y = 40000

let world_x = parent.world_x
let world_y = parent.world_y

set_private_co_ords

let counter = 0

let draw_override = DRAW_OVERRIDE_OVERRIDE_ALWAYS

start
	let INTERPOLATION_X_PERCENTAGE = parent.progress / 200
end

