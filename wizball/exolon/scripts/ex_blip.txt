	let Bitshift = 0
	SET_PRIVATE_CO_ORDS

	let draw_order = draw_order + 1

	let sprite = level_scenery_sprites[arb]
	let base_frame = 69
	let current_frame = base_frame
	let draw_mode = DRAW_MODE_SPRITE
	let opengl_booleans = OPENGL_BOOLEAN_BLEND_ADD + OPENGL_BOOLEAN_SCALE

	let lifetime = 10
	
start

	if lifetime > 7 then
		let opengl_scale_x = opengl_scale_x % 15000
	else
		let opengl_scale_x = opengl_scale_x % 5000
	end_if

	let opengl_scale_y = opengl_scale_x

	let lifetime = lifetime - 1

	if lifetime = 0 then
		kill_entity own_id
	end_if
end
