	let entity_type = entity_type + ENT_TYPE_DISPOSABLE
	let entity_type = entity_type + ENT_TYPE_SLEEPABLE

	let Bitshift = 0
	SET_PRIVATE_CO_ORDS
	let sprite = pickup_sprites[set][32][32][16][16]
	let base_frame = 0
	let frame_count = 4
	let current_frame = base_frame
	let draw_mode = DRAW_MODE_SPRITE

	let collide_type = ENT_TYPE_PICKUP
	let collide_with = ent_type_player

	let COLLISION_SHAPE = RECTANGLE
	SET_COLLISION_FROM_FRAME

	let upper_width = upper_width - 8
	let lower_width = lower_width - 8

	entity_hitline gosub award_bullets
start
end

.award_bullets

	let collided_entity.GUN_1_AMMO_COUNT = 99
	// Do some nice particle effect here...

	PLAY_SOUND pickup_sptang 255 22050 128

	change_script ex_pickup_disappear