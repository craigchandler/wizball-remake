let draw_order = 3
let draw_mode = draw_mode_sprite

let opengl_booleans = opengl_booleans + opengl_boolean_scale
let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_VERTEX_SECONDARY_COLOUR
let opengl_booleans = opengl_booleans + opengl_boolean_filtered

let opengl_vertex_red = 0
let opengl_vertex_green = 0
let opengl_vertex_blue = 0

let sprite = large_graphics[arb]

let current_frame = 0

let world_x = 320
let world_y = 240

set_private_co_ords

let entity_type = entity_type + ENT_TYPE_DISPOSABLE

start

	if parent.signal = 1 then
		goto shrink
	end_if

end

.shrink

start
	let temp_1 = 10000 - opengl_scale_y
	let temp_2 = 10000 - opengl_scale_x
	let temp_1 = temp_1 + temp_2
	let temp_1 = temp_1 * 255
	let temp_1 = temp_1 / 20000

	let opengl_vertex_red = temp_1
	let opengl_vertex_green = opengl_vertex_red 
	let opengl_vertex_blue = opengl_vertex_red 

	if opengl_scale_y > 80 then
		let opengl_scale_y = opengl_scale_y % 7000
	else_if opengl_scale_x > 80 then
		let opengl_scale_x = opengl_scale_x % 7000
	else
		goto blink
	end_if
end

.blink
	spawn_entity ex_blip 0 0 process_next

	kill_entity own_id

