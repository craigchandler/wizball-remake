#define slice_number		passed_param_1

#define wobble_size		v0
#define wobble_counter		v1

	let draw_order = 20

	let sprite = large_graphics[arb]
	let current_frame = 106 - slice_number
	let temp_1 = slice_number * 8
	let world_x = 540 - temp_1

	SET_PRIVATE_CO_ORDS

	let temp_1 = current_frame - 52

	wait temp_1

	let lifetime = 200 - temp_1

	let temp_1 = 320 - world_x
	let temp_1 = abs temp_1
	let temp_1 = temp_1 / 8
	let lifetime = lifetime + temp_1

	let draw_mode = DRAW_MODE_SPRITE
	let opengl_booleans = OPENGL_BOOLEAN_BLEND_ADD
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_SCALE
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_ROTATE
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_VERTEX_COLOUR_ALPHA

	let opengl_vertex_alpha = 0
	let opengl_scale_y = 0

	let wobble_size = 10000
	let wobble_counter = 9000
	
start

	let opengl_vertex_alpha = opengl_vertex_alpha + 15 !> 255

	let wobble_counter = wobble_counter + 200

	let opengl_angle = wobble_size sin wobble_counter

	let opengl_scale_y = opengl_scale_y + 200 !> 10000

	let wobble_size = wobble_size - 100 !< 0

	let lifetime = lifetime - 1 !< 0

	if lifetime = 0 then
		goto next_phase
	end_if

end

.next_phase

start

	let opengl_scale_y = opengl_scale_y % 9000

	if opengl_scale_y = 1 then
		kill_entity own_id
	end_if

end