#define behaviour		v0
#define fade_up			0
#define stable			1
#define fade_down		2
#define colour_cycle		v1
#define base_world_y		v2

	let entity_type = entity_type + ENT_TYPE_DISPOSABLE

	let Bitshift = 4
	SET_PRIVATE_CO_ORDS

	let draw_mode = DRAW_MODE_SPRITE

	let opengl_booleans = opengl_boolean_blend_add + opengl_boolean_vertex_colour_alpha
	let opengl_booleans = opengl_booleans + opengl_boolean_scale
	let opengl_booleans = opengl_booleans + opengl_boolean_filtered

//	let opengl_scale_x = 5000
//	let opengl_scale_y = 5000	

	let sprite = game_font[set][32][32][16][16]

	let x_Vel = -48

	let base_world_y = world_y

	let behaviour = fade_up

start

	let temp_1 = world_x % 3500
	let temp_1 = temp_1 + parent.colour_cycle
	let temp_1 = temp_1 mod 360

	GET_VERTEX_COLOURS_FROM_HSL temp_1 256 128

	let temp_1 = temp_1 * 200
	let temp_2 = 24 sin temp_1
	let world_y = base_world_y + temp_2

	if behaviour = fade_up then
		if opengl_vertex_alpha < 255 then
			let opengl_vertex_alpha = opengl_vertex_alpha + 15
		else
			let behaviour = stable
		end_if
	else_if behaviour = fade_down then
		if opengl_vertex_alpha > 0 then
			let opengl_vertex_alpha = opengl_vertex_alpha - 15
		else
			kill_entity own_id
		end_if
	else
		if world_x < -32 then
			kill_entity own_id
		end_if
	end_if

	if parent.signal & destroy_scrolly_message then
		let behaviour = fade_down
	end_if

end
