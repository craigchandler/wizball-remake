	let entity_type = entity_type + ENT_TYPE_DISPOSABLE
	let entity_type = entity_type + ENT_TYPE_SLEEPABLE

	let Bitshift = 0
	SET_PRIVATE_CO_ORDS
	let sprite = level_scenery_sprites[arb]
	let base_frame = 65
	let frame_count = 1
	let current_frame = base_frame
	let draw_mode = DRAW_MODE_SPRITE

	let opengl_booleans = OPENGL_BOOLEAN_BLEND_ADD
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_SCALE

	let lifetime = 15

	let opengl_scale_y = 40000
	let opengl_scale_x = 2000
	
	let draw_order = draw_order + 1

start
	let world_x = parent.world_x
	let world_y = parent.world_y

	set_private_co_ords

	let opengl_scale_y = opengl_scale_y >% 1000 2000
	let opengl_scale_x = opengl_scale_x >% 10000 1000

	let lifetime = lifetime - 1

	if lifetime = 0 then
		kill_entity own_id
	end_if
end
