	LET ENTITY_TYPE = ENT_TYPE_DESTROYED_IN_LEVEL_TRANSITION + ENT_TYPE_DESTROYED_IN_LEVEL_RESET
	LET ENTITY_TYPE = ENTITY_TYPE + ENT_TYPE_DISPOSABLE
	LET ENTITY_TYPE = ENTITY_TYPE OR ENT_TYPE_PAUSABLE

	let Bitshift = 7
	SET_PRIVATE_CO_ORDS

	let sprite = wizball[set][48][48][24][24]
	let base_frame = 0
	let current_frame = base_frame
	let draw_mode = DRAW_MODE_SPRITE
	let opengl_booleans = OPENGL_BOOLEAN_BLEND_MULTIPLY
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_SCALE
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_VERTEX_COLOUR_ALPHA
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_FILTERED

	let draw_order = EXPLOSION_DRAW_ORDER - 1
start
	let current_frame = current_frame + 1 !< 0 !> 63

	let x_vel = x_vel % 9700
	let y_vel = y_vel % 9700

	let opengl_vertex_alpha = opengl_vertex_alpha - 4 !< 0

	let opengl_scale_x = opengl_scale_x % 9900
	let opengl_scale_y = opengl_scale_x

	let lifetime = lifetime - 8

	if opengl_vertex_alpha = 0 then
		kill_entity own_id
	end_if
end
