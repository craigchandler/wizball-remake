	LET ENTITY_TYPE = ENT_TYPE_DESTROYED_IN_LEVEL_TRANSITION + ENT_TYPE_DESTROYED_IN_LEVEL_RESET
	LET ENTITY_TYPE = ENTITY_TYPE + ENT_TYPE_DISPOSABLE
	LET ENTITY_TYPE = ENTITY_TYPE OR ENT_TYPE_PAUSABLE

	let Bitshift = 7
	SET_PRIVATE_CO_ORDS

	let sprite = particle_effects[arb]
	let draw_mode = DRAW_MODE_SPRITE
	let opengl_booleans = OPENGL_BOOLEAN_BLEND_ADD
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_SCALE
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_ROTATE
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_VERTEX_COLOUR_ALPHA

	let current_frame = rand (20,21)

	let lifetime = 0

	let y_acc = 5

	let opengl_scale_x = rand 7000,10000
	let opengl_scale_y = rand 7000,10000

	let opengl_angle = rand (-16000,16000)
	let temp_1 = rand (512,768)
	let x_vel = temp_1 sin opengl_angle
	let y_vel = temp_1 cos opengl_angle

	let draw_order = CATELLITE_DAMAGE_SPARK_DRAW_ORDER

start

	let opengl_angle = atan2 (x_vel,y_vel)

	let x_vel = x_vel % 9500
	let y_vel = y_vel % 9200

	let opengl_scale_x = opengl_scale_x - 100 !< 100
	let opengl_scale_y = opengl_scale_x

	let lifetime = lifetime + 1

	if lifetime > 25 then
		let opengl_vertex_alpha = opengl_vertex_alpha - 5
		if opengl_vertex_alpha <= 0 then
			kill_entity own_id
		end_if
	end_if

end
