#define wizball_entity_id		temp_2

// This is a function script, it's called with the command call_script command.

	let temp_1, temp_2 = GET_WINDOW_MAP_POSITION

	// Okay, now we know where the window is, lets add half the width on.

	LET temp_1 = temp_1 + HALF_SCREEN_WIDTH

	let temp_2 = world_x - temp_1

	let temp_2 = abs temp_2

	if temp_2 < HALF_SCREEN_PLUS_EXIT_PHANTOM_ZONE then
		let returned_value_1 = FALSE
	else
		let returned_value_1 = TRUE
	end_if

return
