#define colour_flag			passed_param_2

	LET ENTITY_TYPE = ENT_TYPE_DESTROYED_IN_LEVEL_TRANSITION + ENT_TYPE_DESTROYED_IN_LEVEL_RESET
	LET ENTITY_TYPE = ENTITY_TYPE + ENT_TYPE_DISPOSABLE
	LET ENTITY_TYPE = ENTITY_TYPE OR ENT_TYPE_PAUSABLE

	SET_PRIVATE_CO_ORDS
	let sprite = paintballs_and_drips[arb]
	let base_frame = colour_flag + 27
	let current_frame = base_frame
	let draw_mode = DRAW_MODE_SPRITE
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_FILTERED
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_SCALE
	let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_ROTATE

	let temp_1 = rand (0,1)

	if temp_1 = 0 then
		let opengl_angle = rand (-3000,3000)
	else
//		let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_ROTATE_CLOCKWISE
//		let opengl_booleans = opengl_booleans + OPENGL_BOOLEAN_HORI_FLIPPED
		let opengl_angle = rand (6000,12000)
	end_if

	let v0 = rand (-1,1)

	let opengl_scale_x = 10000
	let opengl_scale_y = opengl_scale_x

start

	if v0 < 3 then
		let opengl_scale_x = opengl_scale_x % 12000
		let opengl_scale_y = opengl_scale_y % 12000
	else
		let opengl_scale_x = opengl_scale_x % 8000
		let opengl_scale_y = opengl_scale_y % 8000
	end_if


	let v0 = v0 + 1

	if v0 = 10 then
		kill_entity own_id
	end_if
end
